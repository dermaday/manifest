apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: dermaday-production
  namespace: argocd

spec:
  project: default

  source:
    repoURL: git@github.com:dermaday/manifest.git
    targetRevision: main
    path: production

  destination:
    server: https://kubernetes.default.svc
    namespace: production

  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true

    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - FailOnSharedResource=true